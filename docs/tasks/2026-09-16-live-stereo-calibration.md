# 2026-09-16 实时双目标定交接

- 目标：原始高分辨率调焦 → 视频自动角点采集与姿态指引 → 单目/双目求解 → 留出验证及校正检查。
- 工作目录：内层 `Boom_Birds`；设备固定目录 `/home/gmaster/boom_birds_ws/stereo_depth`。
- 版本：基于 `9e3e376` 的未提交工作区；部署 7 个文件已逐个 SHA256 比对一致。未提交、未创建旧工程目录。
- 文件：新增 `calibration_core.py`、`live_calibration.py`、`calibration.html`、`test_live_calibration.py`、`LIVE_CALIBRATION.md`；更新模块 README、depth_preview 的 `--calibration` 入口及状态/需求/BOM 证据。
- 软件验证：Pi OpenCV 4.6.0 / NumPy 1.26.4，`python3 -m unittest -v test_live_calibration` 9 项通过；包括合成几何恢复基线、留出验证、完整结果保存/JSON、首帧等待、旧/新分辨率深度映射兼容。`git diff --check` 通过。
- 实机验证：V4L2 列出 MJPG 2560×960@60；实际 JPEG 和浏览器图像均为 2560×960；取帧短时约 58–59 FPS，原图网页预览默认上限 15 FPS；检测叠加视图加载成功。
- 浏览器验证：无控制台错误；375/414/768/1024/1440 px 布局无水平溢出。当前原图调焦页面，无已采集角点或新候选标定。
- 服务：本次启动 `python3 live_calibration.py`，设备回环端口 8082，Windows SSH 转发本地 18082；跨任务需重新核验进程和隧道是否在线，不保证持续运行。
- 下一步：用户实测纸上 10 格=200 mm，硬板固定、两目调焦后，勾选页面确认开始采集，按指引补齐至少 24 个不同姿态。候选结果不会自动替换旧标定。
- 未验证：真实新棋盘完整标定、镜头焦点是否合适、距离精度、长期吞吐、飞行能力；Pi 5 验证不证明 RK3576 部署。
- 操作限制：代码部署直接覆盖固定目录，但相机标定/角点/原始测量仍保存；无刷写、无飞控参数或执行器操作。

## 后续：缓存清理与预览延迟

- 删除 13 个可重建 VSIX 安装缓存文件，共 143450025 字节；保留已安装扩展、所有源码和测量数据。
- SIGSTOP 暂停 extensionHost/Pylance/Copilot；恢复使用原 `resume_dev_backgrounds.py`，已合并更新设备上的暂停记录。
- 最新帧拉取替代网页连续 MJPEG，单请求在途、视图切换取消旧请求、隐藏标签暂停取图。
- 解码限制为 8 FPS；调焦阶段减少检测频率，正式采集保留 SB 搜索回退。原图尺寸和 JPEG 不变。
- 10 项无硬件回归通过；实机短时 CPU 约 87.7%，此前进程统计约 203%；浏览器一次取帧/解码 121 ms，估计帧龄 130 ms。
- 测试时相机仍约 58.8 FPS；12 次 Windows 拉帧传输中位数 148.7 ms，最大 226.9 ms（测试期间另有浏览器请求）。
- 不声称测得传感器曝光到显示的真实延迟；刷新页面以切换新协议。测试产生的额外浏览器页面关闭，服务与原隧道保留。

## 新标定检查与应用（续接核验 2026-09-17）

- 已将 `calibration/live_20260916_210120_642136/candidate.npz` 设为 `depth_preview.Config` 默认路径，本地和树莓派均已更新。
- 本轮 30 组：24 组训练、6 组留出，两目 12/12 区域及 9/9 中心区域覆盖；9 组倾斜，尺度跨度 3.93。
- 重新从 observations 独立执行同一求解流程，K/D/R/T 和报告一致，旋转矩阵正交且行列式为 1，平移方向适用于当前正视差管线。
- 单目 RMS A/B 为 0.2698/0.2861 px；双目 RMS 0.4018 px；留出垂直 P95 0.4924 px、中位 0.1740 px；无筛查警告。
- 棋盘决定估计基线 67.671804 mm，未强制缩放为历史 65 mm；不能由内部误差推断尺测精度已通过。
- 标定 NPZ SHA256：`d27f24de5ba69546274dc6f502ce27a7b36e3aae5f39220252c9e01af675aa7d`。
- 10 项回归通过。实机保存 `depth_outputs/20260916_211555_262920`，XYZ 为 float32 `(240,320,3)`，Z=depth，无效 XYZ 全 NaN，原图为 2560×960；当时有效比例约 20.2%，只代表该场景。
- `application_check.json`、`runtime_verification.json` 与原始角点/候选/报告已取回本地主工程。
- 续接时旧服务已停止，重新启动默认深度程序与 Windows 18081→Pi 8081 隧道；health 显示新标定、无错误、约 21.44 FPS（短时样本）。
- 浏览器入口 `http://127.0.0.1:18081/`；旧校准服务不再占用相机。后续独立已知距离测试仍待用户摆放实物进行。
