# Companion 开发环境记录

## 2026-09-15 目录整理核验补充

已通过 SSH 读取 `gmaster@192.168.137.200:/home/gmaster/boom_birds_ws/stereo_depth`，完整复制到本项目 `companion/stereo_depth/`；334 个非缓存文件逐项 SHA256 一致。当前已有 `depth_preview.py`、65 mm 标定、原始采集样例、XYZ/深度结果及历史性能记录，之前“尚无本地深度运行资料”的进度描述已由本次导入补充。导入清单见 `workflows/stereo_import_20260915.json`。

本次仅验证文件与 Python 语法，不重新测量深度、帧率或设备系统，不将历史运行记录提升为独立验收。ROS 安装、距离精度与飞行能力仍需对应验证。下文保留历史记录。

记录日期：2026-09-11（初始记录2026-08-25）  
状态：`CURRENT BASELINE（用户报告，待实机核验）`

## 1. 环境基线与历史记录

| 项目 | 当前记录 | 证据状态 |
| --- | --- | --- |
| 用途 | Companion 前期软件与接口验证平台；不代表最终机载计算板选型 | 用户确认 |
| 树莓派型号 | Raspberry Pi 5（Pi 5）；内存容量 TBD | 用户于2026-09-11确认型号 |
| 操作系统 | Ubuntu Server 24.04 | 用户于 2026-08-25 确认；精确镜像版本、架构与内核版本待核验 |
| ROS 2 基线 | ROS 2 Jazzy | 与 Ubuntu 24.04/ARM64 配套的开发基线；尚未确认安装状态 |
| 主机名 | `gmaster` | 用户确认；待通过实机 `hostnamectl` 核验 |
| 登录用户名 | `gmaster` | 用户确认；待通过实机 `id` 核验 |
| 登录密码 | 已记录在本机忽略文件 `../.local/secrets/raspberry-pi-gmaster.txt` | 敏感信息，不写入工程文档或版本控制 |
| SSH 地址 | `192.168.137.200` | 2026-09-15 文件导入时连接成功；后续使用前重新核验 |

## 2. 2026-09-11 进度记录（由顶部导入说明补充）

- 已完成：记录树莓派当前系统类型、主机名、用户名和本机登录凭据；用户已将操作系统版本更正为 Ubuntu Server 24.04。
- 未完成：尚未在本工作区保存 `hostnamectl`、`uname -a`、`/etc/os-release`、CPU 架构、网络地址、SSH 登录、ROS 2 安装状态或 Companion 软件运行日志。
- 当前树莓派只用于 Companion 侧的软件链、感知、日志和 MAVLink2/Offboard 前期验证；最终机载计算板仍保持 `CANDIDATE`。
- 2026-09-11用户进一步确认：当前使用 Pi 5 验证双目转深度，RK3576 为后续迁移计算平台；具体板卡与系统仍 TBD。工程应保留独立的采集、校正、匹配和深度输出接口，便于后续迁移；这不是 RK3576 部署成功的证明。
- PX4 继续承担姿态、控制分配、DShot/PWM、日志与安全接管；树莓派不得直接控制电机。

## 3. 下次连接后的核验命令

```bash
hostnamectl
id
cat /etc/os-release
dpkg --print-architecture
uname -a
ip -brief address
command -v ros2 || true
printenv ROS_DISTRO
```

预期系统版本为 Ubuntu 24.04（`noble`），架构需按实机输出记录；若安装 ROS 2，当前基线为 `jazzy`。核验输出应作为日期化日志保存；在获得实机输出前，不把本记录提升为 `PASS`。

## 4. USB 双目转深度输入背景（2026-09-11）

| 项目 | 当前记录 | 证据状态 |
| --- | --- | --- |
| 相机接口 | USB免驱摄像头；具体USB版本、设备节点与驱动枚举 TBD | 用户确认免驱，待实机枚举 |
| 同步 | 左右硬件同帧同步 | 用户确认；同步误差与曝光时序尚无日志 |
| 图像布局 | 同一帧左右拼接双目图像 | 用户确认；实际左右顺序、单目尺寸及是否带边框 TBD |
| 当前目标 | 双目图像转换为实际距离深度图 | 用户明确；不将VIO/SLAM或点云作为本轮交付目标 |
| 处理平台 | 当前 Pi 5 / Ubuntu Server 24.04 验证；后续迁移到 RK3576 | RK3576板卡、系统、RKNN环境 TBD |
| 相机参数 | 型号、传感器、全局/卷帘快门、基线、镜头/FOV、总分辨率、FPS、MJPEG/YUYV等格式 TBD | 待用户补充或设备资料 |
| 标定 | 左右内参、畸变、外参、校正参数及对应分辨率 TBD | 未提供标定文件 |
| 验收指标 | 目标距离区间、误差容限、深度图分辨率/最低FPS、端到端最大延迟 TBD | 待用户定义；不以示例参数作为验收要求 |
| 系统角色 | Pi 5 为当前验证平台，RK3576 为后续迁移平台；相机安装方向、是否兼任VIO TBD | 待用户确认 |

实施约束：先拆分同帧图像再校正；缩放后同步更新标定几何与视差尺度；输出深度须明确单位和无效值。硬件同步不等于全局快门，也不代表已经校正或自带深度输出。保留原始拼接样例及采集时间戳用于复现；USB解码、校正、匹配和发布均纳入延迟测试。
