# 合成场景参数（TEST-ONLY，供分层验证使用）
scene:
  wall_x_m: 3.0
  camera_z_m: 1.5
  obstacle:
    x_m: 1.8
    y_m: 0.0
    z_m: 1.5
    half_y_m: 0.6
    half_z_m: 0.4
camera_motion:
  y_amplitude_m: 1.0        # 虚拟相机沿世界 +y 往复，用于「静态障碍不动」检查
  period_s: 12.0
depth_gate:
  min_depth_m: 0.2
  max_depth_m: 5.0
ego:
  map_size: [20.0, 20.0, 5.0]
  resolution_m: 0.1
  obstacles_inflation_m: 0.15
  ground_height_m: -0.01
  goal: [3.0, 0.0, 1.2]
